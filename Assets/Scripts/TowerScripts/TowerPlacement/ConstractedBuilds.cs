using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConstractedBuilds : MonoBehaviour
{
   // public Dictionary<GameObject, GameObject> constructedBuildings = new Dictionary<GameObject, GameObject>();

    //public static ConstractedBuilds Instance { get; private set; }


    //// Ýki bina arasýnda baðlantý eklemek için
    //public void AddBuilding(GameObject a, GameObject b)
    //{
    //    if (!constructedBuildings.ContainsKey(a))
    //    {
    //        constructedBuildings[a] = b;
    //        Debug.Log($"Connection added: {a.name} -> {b.name}");
    //    }
    //    else
    //    {
    //        Debug.LogWarning($"Connection already exists for {a.name}");
    //    }
    //}


}
